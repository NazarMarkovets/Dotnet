namespace Dotnet.Lib
{
    internal class BinaryTree
    {
        public readonly TreeNode treeRoot;
        public BinaryTree(int key)
        {
            treeRoot = new TreeNode(key);
        }
    }
    
}
